# 异常机制

## 元信息

| 项 | 值 |
|----|-----|
| 板块 | java-basics |
| 难度 | 入门 |
| 预估时长 | 3 小时 |
| 前置 | [03-面向对象](03-面向对象.md) |

## 学习目标

- [ ] 区分 Checked 与 Unchecked 异常
- [ ] 正确使用 `try-catch-finally` 与 try-with-resources
- [ ] 能定义业务异常并合理抛出
- [ ] 理解异常链与 `getCause()`

## 核心概念

### 定义

异常是程序运行时错误的对象表示。Checked 异常必须处理或声明；`RuntimeException` 及其子类为 Unchecked。

### 常见误区

- 捕获 `Exception` 后空处理（吞异常）
- 在 `finally` 中 `return` 覆盖 try 返回值
- 用异常控制正常业务流程

## 关键 API / 注解

| 名称 | 说明 |
|------|------|
| `Throwable` / `Exception` / `RuntimeException` | 层次 |
| `try-with-resources` | 自动关闭 `AutoCloseable` |
| `throws` | 声明 Checked 异常 |

## 对应代码

| 类 | 路径 |
|----|------|
| `ExceptionDemo` | [`ExceptionDemo.java`](../../java-basics/jb-04-exception/src/main/java/com/lean/exception/ExceptionDemo.java) |
| `DivideService` / `BusinessException` | 同包 `com.lean.exception` |
| 验收测试 | [`DivideServiceTest.java`](../../java-basics/jb-04-exception/src/test/java/com/lean/exception/DivideServiceTest.java) |

## 子项目规格

| 字段 | 值 |
|------|-----|
| **moduleId** | `jb-04-exception` |
| **path** | `java-basics/jb-04-exception/` |
| **artifactId** | `jb-04-exception` |

### 包结构

| 类 | 说明 |
|----|------|
| `com.lean.exception.BusinessException` | 自定义运行时异常 |
| `com.lean.exception.DivideService` | `divide(a,b)` 除零抛异常 |
| `com.lean.exception.DivideServiceTest` | 断言抛出与消息 |
| `com.lean.exception.ExceptionDemo` | `main` 演示 try-with-resources |

### 运行与验证

```bash
cd java-basics/jb-04-exception
mvn -q exec:java
mvn test
```

## 参考资料

- [Exceptions](https://docs.oracle.com/javase/tutorial/essential/exceptions/)
